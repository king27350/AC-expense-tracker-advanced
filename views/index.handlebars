<form action="/records">
  <div class="input-group-append">
    <button class="btn btn-major" type="submit" id="search-button"><i class="fas fa-search"></i></button>
    <div class="dropdown btn-sort">
      <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown"
        aria-haspopup="true" aria-expanded="false">
        Sort
      </a>
      <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
        <button class="dropdown-item" name="sort" value="asc">A-Z</button>
        <button class="dropdown-item" name="sort" value="desc">Z-A</button>
        <button class="dropdown-item" name="sort" value="-1">金額大-小</button>
        <button class="dropdown-item" name="sort" value="1">金額小-大</button>
      </div>
    </div>

  </div>
  </div>
</form>

<div class="container">
  <div class="row">
    <div class="col-10  mx-auto text-center bg-white">
      {{!-- total amount --}}
      <div class="col-10 mx-auto d-block mb-3">
        <h4 class="text-left">總金額：</h4>
        <h1 class="text-right text-primary">$ {{totalAmount}}</h1>
      </div>
      {{!-- /total amount --}}

      {{#each records}}
      <div class="d-inline-block col-5">
        <h4>{{this.name}}</h4>
        <span>{{this.date}}</span>
      </div>
      <div class=" d-inline-block col-1 my-auto mr-5">
        <span style="font-size: 20px; line-height:60px;">{{this.amount}}</span>
      </div>

      <div class="mt-2">
        <a class="btn btn-outline-primary" href="/records/{{this._id}}/edit">編輯</a>

        <form action="/records/{{{this.id}}}/delete?_method=DELETE" method="POST" style="display: inline;">
          <button class="btn btn-outline-danger" type="submit">delete</button>
        </form>
      </div>

      {{/each}}
      <div class="col-10 mx-auto d-block mb-3">
        <a class="btn btn-primary mt-5" href="/records/new">新增支出</a>
      </div>
    </div>
  </div>
</div>